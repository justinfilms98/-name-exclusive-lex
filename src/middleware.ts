// =====================================================
// NEXT.JS MIDDLEWARE
// Handles route protection and authentication
// =====================================================

export { default } from "next-auth/middleware"
// export const config = { matcher: ["/collections", "/upload", "/admin"] }
// Temporarily allow all routes to test login session state 